// This route group file is intentionally minimal — the homepage is in app/page.tsx
// Both resolve to / but Next.js uses app/page.tsx as the authoritative handler
export default function MarketingPage() {
  return null
}
